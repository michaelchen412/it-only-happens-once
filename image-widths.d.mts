// Declarations for image-widths.mjs — the file itself stays .mjs so
// astro.config.mjs (also .mjs, bundled before src/ exists to it) can import it
// without asking the config loader to transpile TypeScript.
export declare const IMAGE_WIDTHS: number[];
